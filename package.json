{
  "name": "tetra-ui",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "check:format:fix": "npx ultracite fix",
    "check:format": "npx ultracite check",
    "check:types": "turbo run check:types",
    "clean": "git clean -xdf node_modules",
    "dev": "turbo run dev"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.5",
    "husky": "^9.1.7",
    "turbo": "^2.6.1",
    "typescript": "~5.8.3",
    "ultracite": "6.3.3"
  },
  "packageManager": "pnpm@10.12.4",
  "engines": {
    "node": ">=18"
  }
}
