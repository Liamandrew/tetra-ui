---
title: Chip
description: Displays a compact, rounded chip element.
---

<ComponentPreview name="chip" type="png" />

## Installation

<Tabs defaultValue="cli">

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```package-install
npx shadcn@latest add @tetra-ui/chip
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="chip" title="components/ui/chip.tsx" />

</Steps>

</TabsContent>

</Tabs>


## Usage

```tsx
import { Chip, ChipIcon, ChipText } from "@/components/ui/chip";
import { XIcon } from "@/components/ui/icons";
```

```tsx
<Chip>Chip</Chip>
```

## Variants

```tsx
<Chip variant="default">Default</Chip>
<Chip variant="secondary">Secondary</Chip>
<Chip variant="destructive">Destructive</Chip>
<Chip variant="outline">Outline</Chip>
```

## With icon

Chips are pressable by default. Add a trailing icon for dismissible or actionable chips:

```tsx
<Chip onPress={() => removeTag(id)}>
  <ChipText>React Native</ChipText>
  <ChipIcon>
    <XIcon />
  </ChipIcon>
</Chip>
```

## Disabled

Pass `disabled` to mark the chip as disabled for accessibility (`accessibilityState`). It does not currently block presses or change styles:

```tsx
<Chip disabled>Unavailable</Chip>
```

## API Reference

### Chip

Extends [React Native Pressable](https://reactnative.dev/docs/pressable#props) props. String children are wrapped in `ChipText` automatically.

<TypeTable
  type={{
    variant: {
      description: "Visual style of the chip.",
      type: '"default" | "secondary" | "destructive" | "outline"',
      default: '"default"',
    },
    disabled: {
      description: "Sets `accessibilityState.disabled`. Not forwarded to Pressable and does not apply disabled styles.",
      type: "boolean",
    },
  }}
/>

### ChipText

Renders chip label text styled from the parent `Chip` context.

<TypeTable
  type={{
    children: {
      description: "Label content.",
      type: "React.ReactNode",
      required: true,
    },
    className: {
      description: "Additional Tailwind classes.",
      type: "string",
    },
  }}
/>

### ChipIcon

Clones a single child icon with size and color from the parent `Chip` context.

<TypeTable
  type={{
    children: {
      description: "A single icon element.",
      type: "React.ReactNode",
      required: true,
    },
    className: {
      description: "Additional Tailwind classes.",
      type: "string",
    },
  }}
/>
