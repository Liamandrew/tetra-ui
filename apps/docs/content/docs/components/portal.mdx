---
title: Portal
description: Renders children into a different part of the app.
---

## Installation

<Tabs defaultValue="cli">

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```package-install
npx shadcn@latest add @tetra-ui/portal
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="portal" title="components/ui/portal.tsx" />

</Steps>

</TabsContent>

</Tabs>

## Usage

```tsx
import { PortalHost } from "@/components/ui/portal";
```

```tsx
export default function RootLayout() {
  return (
    <>
      <Stack />
      <PortalHost />
    </>
  );
}
```

## When to use

Add `PortalHost` once at the root of your app. Components that render into a portal — such as [Select](/docs/components/select) (popover mode) and [Popover](/docs/components/popover) — require it to display content above the rest of the UI.

Place `PortalHost` as a sibling to your navigation stack, not inside individual screens.

## API Reference

### PortalHost

Mount point where portal content is rendered. Place once at the app root.

<TypeTable
  type={{
    name: {
      description: "Host name. Must match `hostName` on `Portal` when using a custom host.",
      type: "string",
      default: '"TETRA_UI_DEFAULT_HOST_NAME"',
    },
  }}
/>

### Portal

Renders `null` in place. Injects `children` into the matching `PortalHost`.

<TypeTable
  type={{
    name: {
      description: "Unique name for this portal entry.",
      type: "string",
      required: true,
    },
    hostName: {
      description: "Target host name.",
      type: "string",
      default: '"TETRA_UI_DEFAULT_HOST_NAME"',
    },
    children: {
      description: "Content to render inside the host.",
      type: "React.ReactNode",
      required: true,
    },
  }}
/>

### PortalOverlay

Platform wrapper used by overlays: `FullWindowOverlay` on iOS, `Fragment` elsewhere.

