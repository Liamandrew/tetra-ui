---
title: Native Date Select
description: A native date and time selection input built on Expo UI DatePicker and DateTimePicker, with optional input chrome and platform-aware presentation.
---

Native Date Select wraps Expo UI's [SwiftUI DatePicker](https://docs.expo.dev/versions/latest/sdk/ui/swift-ui/datepicker/) on iOS and [Jetpack Compose DateTimePicker](https://docs.expo.dev/versions/latest/sdk/ui/jetpack-compose/datetimepicker/) on Android. Without Trigger/Input/Content, it renders the native picker inline. With the composed API, iOS defaults to a wheel bottom sheet (`display="compact"` embeds the native compact picker in input chrome); Android mounts a Material date/time dialog.

<ComponentPreview name="native-date-select" type="gif" />

## Installation

<Tabs defaultValue="cli">

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```package-install
npx shadcn@latest add @tetra-ui/native-date-select
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Install the following dependencies:</Step>

```package-install
npm install @expo/ui react-native-reanimated
```

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="native-date-select" />

</Steps>

</TabsContent>

</Tabs>

## Usage

### Modes

Set `mode` on the root to choose what the picker selects: `date` (default), `time`, or `datetime`.

```tsx
import {
  NativeDateSelect,
} from "@/components/ui/native-date-select";
```

```tsx
const [date, setDate] = useState(new Date());

<NativeDateSelect mode="date" value={date} onValueChange={setDate} />

<NativeDateSelect mode="time" value={date} onValueChange={setDate} />

<NativeDateSelect mode="datetime" value={date} onValueChange={setDate} />
```

Omit Trigger/Input/Content to render the picker directly. On iOS, set `display` to `compact`, `graphical`, or `wheel` (maps to SwiftUI [`datePickerStyle`](https://docs.expo.dev/versions/latest/sdk/ui/swift-ui/datepicker/#styling-with-modifiers)). On Android, inline has no combined datetime UI — use the composed Input API for `mode="datetime"` (sequential date then time dialogs).

### Composed date select

Compose `NativeDateSelectTrigger`, `NativeDateSelectInput`, and `NativeDateSelectContent` like Select. The Trigger opens the picker; Input is display-only chrome; Content owns the sheet/dialog.

Set `display` on the input (or root) to choose the iOS presentation:

- **`wheel` / default (iOS)** — Trigger opens a bottom sheet with a wheel DatePicker
- **`compact` (iOS)** — non-pressable input chrome; only the native compact DatePicker is interactive (Trigger optional)
- **Android** — Trigger mounts a Material dialog (`DatePickerDialog` / `TimePickerDialog`). For `datetime`, date then time dialogs run sequentially. `display` maps to picker/input variant on the dialog.

Pass `InputAddon` children to Input for adornments. On iOS wheel, optionally compose `NativeDateSelectSheetFooter` and `NativeDateSelectSheetConfirm` inside Content to require an explicit confirm before committing (dismiss cancels).

```tsx
import { Button } from "@/components/ui/button";
import {
  NativeDateSelect,
  NativeDateSelectContent,
  NativeDateSelectInput,
  NativeDateSelectSheetConfirm,
  NativeDateSelectSheetFooter,
  NativeDateSelectTrigger,
} from "@/components/ui/native-date-select";
```

```tsx
<NativeDateSelect mode="date" value={date} onValueChange={setDate}>
  <NativeDateSelectTrigger asChild>
    <NativeDateSelectInput placeholder="Pick a date" />
  </NativeDateSelectTrigger>
  <NativeDateSelectContent>
    <NativeDateSelectSheetFooter>
      <NativeDateSelectSheetConfirm asChild>
        <Button>Confirm</Button>
      </NativeDateSelectSheetConfirm>
    </NativeDateSelectSheetFooter>
  </NativeDateSelectContent>
</NativeDateSelect>
```

```tsx
<NativeDateSelect mode="date" value={date} onValueChange={setDate}>
  <NativeDateSelectInput display="compact" placeholder="Pick a date" />
  <NativeDateSelectContent />
</NativeDateSelect>
```

Without a sheet footer, iOS wheel commits the value as it changes.

Use `formatValue` to customize the displayed label (defaults to `Intl.DateTimeFormat` by mode):

```tsx
<NativeDateSelectInput
  formatValue={(d) => d.toLocaleDateString()}
  placeholder="Pick a date"
/>
```

### Custom trigger

Use any pressable as the Trigger child via `asChild`:

```tsx
import { Button, ButtonText } from "@/components/ui/button";
import {
  NativeDateSelect,
  NativeDateSelectContent,
  NativeDateSelectTrigger,
} from "@/components/ui/native-date-select";
```

```tsx
<NativeDateSelect mode="date" value={date} onValueChange={setDate}>
  <NativeDateSelectTrigger asChild>
    <Button size="sm" variant="secondary">
      <ButtonText>{label ?? "Pick a date"}</ButtonText>
    </Button>
  </NativeDateSelectTrigger>
  <NativeDateSelectContent />
</NativeDateSelect>
```

## Bounds

Constrain selectable dates with `minimumDate` and `maximumDate`. On iOS these map to SwiftUI DatePicker [`range`](https://docs.expo.dev/versions/latest/sdk/ui/swift-ui/datepicker/#with-date-range); on Android they map to Compose `selectableDates`. This limits which dates can be chosen — it is not a dual start/end *value*.

```tsx
<NativeDateSelect
  mode="date"
  value={date}
  onValueChange={setDate}
  minimumDate={new Date(2024, 0, 1)}
  maximumDate={new Date(2024, 11, 31)}
>
  <NativeDateSelectTrigger asChild>
    <NativeDateSelectInput placeholder="Pick a date" />
  </NativeDateSelectTrigger>
  <NativeDateSelectContent />
</NativeDateSelect>
```

## Range selection

Expo UI pickers still select a single date. For a start–end range value, compose two pickers and cross-constrain with `minimumDate` / `maximumDate`:

```tsx
const [start, setStart] = useState<Date>();
const [end, setEnd] = useState<Date>();

<NativeDateSelect
  mode="date"
  value={start}
  onValueChange={setStart}
  maximumDate={end}
>
  <NativeDateSelectTrigger asChild>
    <NativeDateSelectInput placeholder="Start date" />
  </NativeDateSelectTrigger>
  <NativeDateSelectContent />
</NativeDateSelect>

<NativeDateSelect
  mode="date"
  value={end}
  onValueChange={setEnd}
  minimumDate={start}
>
  <NativeDateSelectTrigger asChild>
    <NativeDateSelectInput placeholder="End date" />
  </NativeDateSelectTrigger>
  <NativeDateSelectContent />
</NativeDateSelect>
```

## Controlled

Pass `value` and `onValueChange` for controlled selection. Optionally control `open` and `onOpenChange` for the sheet/dialog:

```tsx
const [date, setDate] = useState(new Date());
const [open, setOpen] = useState(false);

<NativeDateSelect
  value={date}
  onValueChange={setDate}
  open={open}
  onOpenChange={setOpen}
>
  <NativeDateSelectTrigger asChild>
    <NativeDateSelectInput placeholder="Pick a date" />
  </NativeDateSelectTrigger>
  <NativeDateSelectContent />
</NativeDateSelect>
```

## Disabled

```tsx
<NativeDateSelect disabled value={date} onValueChange={setDate}>
  <NativeDateSelectTrigger asChild>
    <NativeDateSelectInput placeholder="Pick a date" />
  </NativeDateSelectTrigger>
  <NativeDateSelectContent />
</NativeDateSelect>
```

## API Reference

Built on [Expo UI](https://docs.expo.dev/versions/latest/sdk/ui/) DatePicker (iOS) / DateTimePicker and Material dialogs (Android). Without Trigger/Input/Content, renders an inline picker; with them, uses form chrome and sheet/dialog.

### NativeDateSelect

Root value/open state for the date/time picker.

<TypeTable
  type={{
    value: {
      description: "Controlled date. Uncontrolled when omitted.",
      type: "Date",
    },
    onValueChange: {
      description: "Called when a date is committed.",
      type: "(value: Date) => void",
    },
    open: {
      description: "Controlled open state for sheet/dialog UI. Uncontrolled when omitted (starts closed).",
      type: "boolean",
    },
    onOpenChange: {
      description: "Called when open state should change.",
      type: "(open: boolean) => void",
    },
    mode: {
      description: "Which components the picker shows.",
      type: '"date" | "time" | "datetime"',
      default: '"date"',
    },
    display: {
      description: "Picker style. On Android, `wheel` maps to the input variant; others use the picker variant.",
      type: '"default" | "wheel" | "compact" | "graphical"',
      default: '"default"',
    },
    minimumDate: {
      description: "Earliest selectable date.",
      type: "Date",
    },
    maximumDate: {
      description: "Latest selectable date.",
      type: "Date",
    },
    is24Hour: {
      description: "Prefer 24-hour time where supported.",
      type: "boolean",
    },
    disabled: {
      description: "Disables interaction.",
      type: "boolean",
    },
    className: {
      description: "Classes on the inline picker host when rendering without the composed API.",
      type: "string",
    },
  }}
/>

### NativeDateSelectTrigger

Opens the sheet/dialog. Extends [React Native Pressable](https://reactnative.dev/docs/pressable#props) props. Must be used within `NativeDateSelect`.

<TypeTable
  type={{
    asChild: {
      description: "Merges props onto the child via `Slot.Pressable` instead of rendering a Pressable.",
      type: "boolean",
    },
  }}
/>

### NativeDateSelectInput

Display-only form chrome. Compose inside `NativeDateSelectTrigger` for wheel/Android. iOS `compact` embeds the native compact DatePicker and does not need a Trigger.

<TypeTable
  type={{
    display: {
      description: "Overrides root `display` for this input.",
      type: '"default" | "wheel" | "compact" | "graphical"',
    },
    placeholder: {
      description: "Placeholder / default sheet title text.",
      type: "string",
      default: '"Pick a date"',
    },
    formatValue: {
      description: "Custom label formatter. Defaults to `Intl.DateTimeFormat` by `mode`.",
      type: "(date: Date) => string",
    },
    className: {
      description: "Additional Tailwind classes on the input chrome.",
      type: "string",
    },
  }}
/>

### NativeDateSelectContent

Owns presentation: iOS wheel bottom sheet, or Android Material dialog. Hosts optional sheet footer.

<TypeTable
  type={{
    title: {
      description: "Sheet title. Falls back to Input placeholder when omitted.",
      type: "string",
    },
  }}
/>

### NativeDateSelectSheetFooter

Accepts [`BottomSheetFooter`](/docs/components/bottom-sheet) props. When present on iOS, picker changes are drafted until confirm; dismiss cancels.

### NativeDateSelectSheetConfirm

Extends [React Native Pressable](https://reactnative.dev/docs/pressable#props) props. Commits the draft date and closes the sheet. Must be used within `NativeDateSelect`.

<TypeTable
  type={{
    asChild: {
      description: "Merges props onto the child via `Slot.Pressable` instead of rendering a Pressable.",
      type: "boolean",
    },
  }}
/>

## Changelog

### 2026-08-04 - Trigger and Content composition

Add `NativeDateSelectTrigger` and `NativeDateSelectContent`. `NativeDateSelectInput` is display-only chrome; open the picker via Trigger (or use any pressable with `asChild`). Sheet footer moves into Content.
