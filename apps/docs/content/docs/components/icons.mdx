---
title: Icons
description: Displays icons.
---

By default, tetra-ui uses icons from [Lucide](https://lucide.dev/icons/), but you can use any SVG icon library you want.

The approach we recommend is to create a styled SVG component with the help of the [withUniwind](https://docs.uniwind.dev/api/with-uniwind) function that you can use throughout your application.

## Installation

<Tabs defaultValue="cli">

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```package-install
npx shadcn@latest add @tetra-ui/icons
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Install the following dependencies:</Step>

```package-install
npm install react-native-svg lucide-react-native
```

<Step>Create a styled SVG component</Step>

```tsx title="components/ui/icons/styled-icon.tsx"
import type { LucideIcon } from "lucide-react-native";
import Svg from "react-native-svg";
import { withUniwind } from "uniwind";
import { cn } from "@/lib/utils";

const styledSvgOptionMapping = {
  style: {
    fromClassName: "className",
  },
  height: {
    fromClassName: "className",
    styleProperty: "height",
  },
  width: {
    fromClassName: "className",
    styleProperty: "width",
  },
} as const;

export const StyledSvg = withUniwind(Svg, styledSvgOptionMapping);

export const createStyledSvg = (Icon: LucideIcon): LucideIcon => {
  const UniwindIcon = withUniwind(Icon, styledSvgOptionMapping);

  const StyledSvg = ({
    className,
    ...props
  }: React.ComponentProps<typeof UniwindIcon>) => {
    return (
      <UniwindIcon
        className={cn("size-6 text-foreground", className)}
        {...props}
      />
    );
  };

  return StyledSvg as LucideIcon;
};
```

<Step>Convert your favourite SVG icon library to a styled React Native SVG component</Step>

```tsx title="components/ui/icons/search.tsx"
import {
  Check,
  ChevronDown,
} from "lucide-react-native";
import { createStyledSvg } from "./styled-icon";

export const CheckIcon = createStyledSvg(Check);
export const ChevronDownIcon = createStyledSvg(ChevronDown);
// ... and so on
```

</Steps>

</TabsContent>

</Tabs>

## Usage

```tsx
import { BadgeCheckIcon } from "@/components/ui/icons";
```

```tsx
<BadgeCheckIcon />
```

## Changelog

### 2025-12-03 Updated Styling

Updated the configuration for `withUniwind` so that stroke classes are used.

### 2026-02-12 Added Lucide Icons

Use Lucide icons as the default icon set.